PG_PASSWORD=quanchan_pqc_2026
HTTPS_PORT=443
GRPC_PORT=50051
# Store the real production .env outside the repo, for example:
#   /etc/quanchan/.env
#
# Store the real production certificate set outside the repo too, for example:
#   /etc/quanchan/server.crt
#   /etc/quanchan/server.key
#   /etc/quanchan/ca.crt
#
# Optional override if your certs live somewhere else:
# QUANCHAN_CERTS_DIR=/etc/quanchan
